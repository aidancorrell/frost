# Test Fixtures

This directory contains sample Iceberg metadata files for unit testing.

Fixtures will be added as we implement real metadata JSON parsing in frost-core.
For now, unit tests use the `test_helpers::make_test_metadata()` builder.
